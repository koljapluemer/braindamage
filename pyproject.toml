[project]
name = "braindamage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.18.5",
    "duckdb>=1.5.5",
    "pydantic>=2.9",
    "python-dotenv>=1.2.2",
    "sqlalchemy>=2.0.51",
    "textual>=0.89",
]

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
