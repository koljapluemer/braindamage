below the plot in the Contract modal, display a table with the top 3 float ranges one could optimize for when buying, displaying min float to stay w/in range, max float, expected price, outcome, ROI, cvar.

Use the best range to inform what values this contract shows in its overview stats on top, and in the contract overview table.

It may be worth it to migrate data model names or even structure to architect this cleanly (or it may not).

#collection