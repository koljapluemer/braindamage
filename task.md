add a new button to top of web-ext sidebar "Overview". This should open a custom HTML page in a new tab. In it, we should have a list of all possible mono trades, in a minimal fashion. For each collection, have one line: name of collection, and then either the EV of the persisted simulated contract (as in, the contract based on actual offers generateable in the sidebar and persisted on disk) w/ the most positive EV; if positive, write this line in bold. If no contract of this kind simulated yet, use instead the best/least-worst naive per-wear bucket contract simulation, like the top table in the sidebar (but only again showing the EV of this contract). In case this backup is used, make the ev cursive. Then, list all possible input skins for this trade, making them clickable links (new tab) and ellipising them after 12 chars. Set all skin names in bold and cursive that are the cheapest skin in this collection in any wear bucket (prefer last-known-offer data, fall back to legacy pricing info, again just like the table in the sidebar) or just cursive when they are in the same decimal range as the cheapest skin in any wear bucket (e.g. last value is $0.30 and cheapest skin is $0.16).
